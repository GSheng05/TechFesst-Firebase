specVersion: "v1beta"
serviceId: "react-notes-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-notes-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
